build_environment: ubuntu1204
language: node_js
node_js:
  - "0.10"

before_install:
  - npm install -g grunt-cli bower

install:
  - cd client
  - npm install
  - bower install

script:
  - cd client
  - grunt
